# Geoguessr, but specifically for the SJSU campus!

## Install instructions for devs:
- (Prerequisite: install npm)
- Clone repo and open a cmd in the project directory
- Run `npm install` to install dependencies
- To start a localhost, run `npm run dev`